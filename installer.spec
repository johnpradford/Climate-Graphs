# PyInstaller specification scaffold

block_cipher = None


a = Analysis(
    ['backend/api/main.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    runtime_hooks=[],
    excludes=[],
)
